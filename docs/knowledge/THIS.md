# THIS — Project & Developer Knowledge

> Living document. Updated by AI whenever a new insight, pattern, or preference is discovered about this project or developer. Do not delete entries — mark outdated ones with `~~strikethrough~~`.

---

## Developer Profile

- Builds with Next.js 16, TypeScript (strict), Tailwind v4 — expects production-grade quality
- Cares deeply about architecture cleanliness and separation of concerns
- Wants concise AI responses — no fluff, no over-explanation
- Prefers dark design systems with strong token discipline

---

## Project Identity

- Template repo — the patterns here become the standard for all derived projects
- Blueprint docs in `docs/blueprint/` are the single source of truth; AI must read them before planning
- Knowledge docs in `docs/knowledge/` capture learned behavior that is *not* in the code

---

## Do's

- Always read relevant blueprint section before planning any change
- Use `@/i18n/navigation` for all routing — never `next/navigation`
- Keep components Server Components by default; add `'use client'` only when required
- Use design tokens exclusively for color — no raw hex/oklch/Tailwind palette utilities
- Every public page needs `buildMetadata()` + `<StructuredData>` — check `SEO_GEO_LLM.md`
- Follow the four-file rule for UI components: impl, stories, test, barrel
- Write compact, high-signal code — no unnecessary comments or abstractions
- Run `npm run lint` (must exit 0) before declaring any task done

---

## Don'ts

- Don't use `useState` for server data — TanStack Query only
- Don't call `fetch` or `apiClient` directly in components — always go through services
- Don't add root `proxy.ts` — use `src/middleware.ts` (export `middleware`) + `src/proxy/` modules instead
- Don't write raw hex colors, oklch values, or Tailwind color utilities in components
- Don't add error handling for impossible cases — only validate at system boundaries
- Don't create README or documentation files unless explicitly asked
- Don't use emojis unless the user explicitly requests them
- Don't summarize what was just done — the user can read the diff

---

## Insights

<!-- Add dated insights here as they are discovered -->
<!-- Format: `[YYYY-MM-DD] insight` -->

- [2026-08-01] **Admin punya typeface sendiri: Inter, bukan Outfit.** Developer menilai Outfit (display face) tidak enak dibaca di admin; Outfit tetap dipakai di sisi publik. Sakelarnya satu aturan CSS di `globals.css`: `body:has([data-admin-shell])` → `var(--font-admin)`, dengan atribut `data-admin-shell` di div root `admin/layout.tsx`. Aturannya sengaja menyasar **`body`**, bukan div admin itu sendiri, karena dialog/listbox/command palette/toast di-portal ke `document.body` (di luar subtree admin) dan kalau tidak akan tertinggal di Outfit; `:has()` bikin ini murni CSS — tanpa client component penempel class, jadi tanpa kedip hidrasi. Jangan pernah menulis `font-sans` eksplisit di komponen admin; pewarisanlah yang bekerja.
- [2026-08-01] **Semua nominal rupiah yang *ditampilkan* lewat `<Money>`** (`src/components/ui/Money/`), bukan rangkaian class per halaman. Dua keputusan yang dikunci di dalamnya: (1) `Rp` adalah satuan — selalu satu tingkat lebih kecil, `font-normal`, `text-foreground-muted`, tidak pernah setebal angkanya; (2) angkanya yang membawa bobot — `font-bold` di ukuran baris/tabel (`inline`, `total`), `font-semibold` di tingkat metrik (`secondary` ke atas). Ini melonggarkan aturan lama "ceiling `font-semibold`" di `DATA_PRESENTATION.md` §2: `font-medium` pada nominal di dalam tabel tenggelam ke label di sebelahnya. `value={null}` merender `—`, nol otomatis muted — jadi tidak perlu ternary di call site. `formatRupiah()` tetap formatter untuk **string** (template WhatsApp, `CardDescription`, `aria-label`), bukan untuk JSX. Spec di `DATA_PRESENTATION.md` §8a.

- [2026-08-01] **Identitas penyewa & penghuni tambahan yang bisa banyak orang.** `Tenant` sekarang menyimpan identitas lengkap (`gender` enum `Gender`, `birthPlace`/`birthDate`, `maritalStatus`, `idAddress` = alamat KTP satu blok teks, `institution` = kantor/kampus) plus **kontak darurat** (`emergencyName`/`emergencyRelation`/`emergencyPhone`) — nomor keluarga adalah satu-satunya jalan saat penyewa sakit/hilang kontak/menunggak, jadi ia field pertama-kelas, bukan catatan bebas. `ContractOccupant` naik dari sekadar `fullName` jadi identitas penuh (`gender`, `phone`, `ktpNumber` **tidak unique** karena bukan pemegang kontrak, `occupation`, `notes`) dan di form jadi **daftar baris yang bisa ditambah tanpa batas** (`OccupantsField.tsx`) — satu kamar boleh 2, 3, atau lebih orang, dan yang dicari saat ada insiden adalah orangnya, bukan jumlahnya. Baris-baris itu dikirim sebagai satu nilai per nama field per baris lalu di-*zip* ulang dengan `formData.getAll()` di Server Action (aman karena `Select` selalu merender hidden input walau kosong, jadi indeksnya tidak pernah geser) — bukan string dipisah koma seperti sebelumnya. Label Indonesia (`genderLabel`, `maritalStatusLabel`, `GENDER_POLICY_LABEL`, `occupantCountLabel`) tinggal di `src/lib/tenant.ts` mengikuti pola `src/lib/electricity.ts`, jangan bikin map lokal per halaman. Gender penyewa **dan seluruh penghuni** dicocokkan ke `Property.genderPolicy`; ketidakcocokan hanya memunculkan peringatan kuning, tidak memblokir submit — pengecualian putra/putri itu keputusan pemilik, bukan form (sama doktrinnya dengan blacklist). Helper `optionalText`/`optionalEnum`/`optionalDate` di `src/lib/validations/tenant.ts` memetakan `''`→`null` supaya field dikosongkan benar-benar terhapus.
- [2026-08-01] **Properti menyimpan lokasi terstruktur, kontak, dan aturan.** `Property.address` sekarang **hanya baris jalan** (jalan, nomor, RT/RW); tingkat administratif jadi kolom sendiri (`district`/`city`/`province`/`postalCode`) supaya list bisa difilter per kota dan alamat pos bisa dicetak utuh — rangkainya lewat `formatPropertyAddress()` di `src/lib/utils.ts`, jangan pernah concat manual. Koordinat (`latitude`/`longitude`, `Decimal(10,7)`) + `mapsUrl` hidup berdampingan, salah satu boleh kosong; `propertyService.getById()` sudah meng-`toNumber()` koordinatnya jadi lapisan di atasnya tidak pernah melihat `Decimal`. Kontak (`contactName`/`contactPhone`/`whatsappNumber`/`contactEmail`) disimpan **per properti**, bukan per perusahaan — `siteConfig` tinggal fallback level brand. Aturan: `genderPolicy` (`PUTRA|PUTRI|CAMPUR`, default CAMPUR karena rumah/villa tak punya aturan gender), `curfewTime` sebagai **teks "HH:MM"** (aturan jam dinding, bukan instant — `DateTime` cuma mengundang bug timezone), dan `rules` teks satu aturan per baris. Helper `optionalText()` di `src/lib/validations/property.ts` memetakan `''`→`null` supaya mengosongkan field benar-benar menghapus nilainya (pola lama `formData.get(x) || undefined` diam-diam mempertahankan nilai lama saat update). Tampilannya: kartu properti dipindah ke `PropertyCard.tsx` (client) — baris info berikon (`MapPin`/`Phone`/`DoorClosed`) dengan `<dl>` di mana ikon adalah `<dt>` ber-`sr-only` label, jumlah kamar dari `_count` di `propertyService.list()`, dan "Nonaktifkan" kini lewat `ConfirmDialog` + `toast` (sebelumnya satu klik submit — melanggar doktrin aksi destruktif). Di form, kolom lokasi jadi controlled hanya untuk merender **preview alamat gabungan** memakai `formatPropertyAddress()` yang sama dengan list/publik, dan "Jam Malam" memakai `Select` (opsi 18:00–04:00 tiap 30 menit) bukan `<input type="time">` yang membawa chrome OS.
- [2026-07-31] **Ukuran kamar/tipe = dimensi, bukan luas.** `Room.sizeSqm`/`RoomType.sizeSqm` (satu angka m²) diganti `lengthM` + `widthM` (masing-masing `Decimal(5,2)`) supaya admin dan calon penyewa tahu bentuknya, bukan cuma totalnya — form kini dua input "Panjang"/"Lebar" (rightAddon `m`, bukan `m²`). Satu formatter `formatRoomSize(lengthM, widthM)` di `src/lib/utils.ts` menghasilkan `"3 m x 4 m"` (null kalau salah satu kosong) — dipakai di halaman publik (`RoomFloorPlan.tsx`); jangan format ulang manual di komponen. Migration manual `20260731160000_room_size_dimensions` me-*rename* kolom lama lalu menambah kolom baru (data lama sizeSqm dipetakan ulang secara eksplisit ke dimensi masuk akal, bukan tebakan otomatis).
- [2026-07-31] **Fasilitas yang bisa di-assign ke kamar/tipe kamar hanya kategori `ROOM`.** `facilityService.listAssignable()` (filter `category: 'ROOM'`) dipakai di keempat halaman `rooms/new`, `rooms/[id]`, `room-types/new`, `room-types/[id]` — bukan `facilityService.list()` yang masih mengembalikan semua kategori (dipakai halaman master fasilitas). `RoomForm`/`RoomTypeForm` tidak lagi mengelompokkan chip per kategori karena facilities yang diterima sudah homogen ROOM. Fasilitas `COMMON` (fasilitas umum properti) di-assign di level `Property` lewat `PropertyFacility`, lintas kamar — lihat kerja paralel yang menambah model itu ke `property.prisma` + `propertyService`/`PropertyForm.tsx` di sesi yang sama.
- [2026-07-31] **Repo ini bisa disentuh sesi/agent lain secara paralel** (lihat entri LEARN 2026-07-28 soal ini) — selama task ini berjalan, sesi lain menambah model `PropertyFacility` + komponen `Table` ke file yang sama (`property.prisma`, `src/lib/validations/property.ts`) di working tree yang sama. Sebelum mengedit file yang juga disentuh migrasi/service besar, jalankan `git status`/`git diff` dulu untuk lihat apa yang sedang ditulis proses lain dan hindari `old_string` yang mengasumsikan bentuk file basi.

- [2026-07-31] Aksi destruktif tidak boleh satu klik: pakai `ConfirmDialog` (`src/components/ui/ConfirmDialog/`) — portal ke `document.body`, dismiss lewat `useDismissable` (Esc + klik luar), fokus awal jatuh ke tombol **Batal**, dan tombol terkunci saat `isPending`. Server Action destruktif mengembalikan `{ error?: string }` supaya kegagalan bisa jadi `toast.error`, bukan `void`. Ikon dari data dirender lewat `<Icon name={kebabCase} />` (`src/components/ui/Icon/`, bungkus `DynamicIcon` lucide) yang memvalidasi nama ke `iconNames` dulu — nama tak dikenal jatuh ke placeholder `circle-dashed`, bukan console error
- [2026-07-31] **Tipe Kamar (`RoomType`) + duplikasi massal.** Aturan yang dipilih developer: fasilitas & foto → *kamar menimpa tipe* (bukan union — kamar yang punya baris sendiri mengabaikan tipe untuk dimensi itu, dievaluasi terpisah antara fasilitas dan foto); harga → tipe hanya *default* yang mengisi form, `Room.price`/`RoomPrice` tetap satu-satunya sumber untuk kontrak & tagihan. Implementasi tunggalnya `resolveInherited()` di `src/lib/room-template.ts`, spec di `DATABASE.md` §Room ↔ RoomType. Duplikat kamar = batch (maks 50) dengan pola nomor `prefix + counter` yang mempertahankan padding nol yang diketik admin, atau daftar nomor manual; foto sengaja tidak ikut tersalin
- [2026-07-31] Migration lewat `prisma migrate dev` **tidak bisa dipakai** di DB cloud ini: shadow DB gagal me-replay `20260727000000_add_multi_property_billing_cycles` (`type "IncidentCategory" already exists`) dan `_prisma_migrations` masih menyimpan record gagal `20260501000000_add_portfolio_votes` milik proyek lain. Cara yang berhasil: tulis `migration.sql` manual → `npx prisma db execute --file ...` → `npx prisma migrate resolve --applied <nama>` → `npx prisma generate`
- [2026-07-31] **Listrik** masuk sebagai `ElectricityMode` = `FREE` | `TOKEN` | `METERED` di `RoomType` **dan** `Room`, mengikuti doktrin harga: tipe cuma *default* yang mengisi form kamar, `Room.electricityMode` yang dibaca saat menagih. Hanya `METERED` yang menambah baris tagihan. Tarif per kWh adalah setting global di tabel `settings` (key-value, whitelist key di `src/lib/settings.ts`, halaman `/admin/settings`, guard `SUPER_ADMIN|KEUANGAN`) — bukan kolom di `Property`. Di `Payment`, `amountDue` tetap satu-satunya angka yang dibaca `getPaymentStatus()`; `rentAmount` menyimpan porsi sewanya supaya pembacaan meter bisa dikoreksi berulang (`amountDue = rentAmount + electricityAmount`), dan `electricityRate` di-*snapshot* per tagihan supaya menaikkan tarif tidak diam-diam mengubah harga tagihan yang sudah dikirim. Aturan murninya di `src/lib/electricity.ts`
- [2026-07-31] `Facility.icon` menyimpan **nama icon lucide-react dalam kebab-case** (mis. `snowflake`, `bed-double`), bukan JSX/emoji — verifikasi nama ke `node_modules/lucide-react/dist/esm/icons/<name>.mjs` sebelum dipakai karena banyak nama intuitif tidak ada (`mirror`, `wardrobe`, `curtains`, `stairs`, `hanger`, `fingerprint`). Master fasilitas dipisah `FacilityCategory` = `COMMON` (fasilitas umum properti) vs `ROOM` (fasilitas kamar); daftar bakunya ada di `prisma/seed.ts` dan idempotent lewat `upsert` by `name`
- [2026-07-31] **Rombak total UI input.** Developer menilai form admin "sangat jelek, gak modern" dan memilih arah **kartu terangkat**: `FormCard` (bg-card + `shadow-card`, header di dalam kartu, field lebar penuh) menggantikan `FormSection`, halaman form `max-w-5xl`, bar simpan `FormStickyBar` menempel di bawah viewport. Semua kontrol native diganti kontrol kustom — `CurrencyInput` (tampil `1.500.000`, kirim `1500000`), `Select` sebagai listbox asli (bukan `<select>`), `DatePicker` berbahasa Indonesia, `Combobox` (angkatan dari `SearchablePicker`), `Checkbox`/`Radio`/`SegmentedControl`/`ChipToggle`. Semuanya menulis nilai ke hidden input supaya `<form action={serverAction}>` tidak berubah. Substrat bersamanya di `src/components/ui/Field/` (`fieldShellVariants` + `Field` + `useFieldIds`) — tinggi field default naik ke `h-11`. `className` sekarang jatuh ke **wrapper** (tempat `sm:col-span-2`), kontrolnya pakai `inputClassName`/`textareaClassName`
- [2026-07-31] Batas doktrin kartu: **input dikartukan, metrik tidak**. `FormCard` untuk grup field; `Metric*` tetap blok tipografis tanpa border/fill. `NewContractForm` menunjukkan keduanya dalam satu layar — step 1–2 kartu input, step 3 konfirmasi berupa `MetricInline` telanjang. Ditulis di `DATA_PRESENTATION.md` §11a
- ~~[2026-07-31] Primitif `Metric*` (`MetricRow`/`MetricBlock`/`MetricValue`/`MetricLabel`/`DeltaPill`/`MetricInline`/`MetricSkeleton`) dan keluarga layout form (`FormLayout`/`FormSection`/`FormGrid`/`FormField`/`FormActions`/`FormError`) plus `Select`/`Textarea`/`Checkbox` sudah dibuat di `src/components/ui/`. Form admin memakai pola editorial: judul+deskripsi section di kolom kiri (`lg+`), field di kanan, dipisah hairline — bukan `<Card>` per grup field, dan halaman form lebarnya `max-w-4xl` (bukan `max-w-2xl` yang memotong kolom judul). `className` pada `Input`/`Select` jatuh ke elemen kontrol, bukan wrapper — untuk `col-span` bungkus dengan `div`~~ (bagian `Metric*` masih berlaku; pola form editorial digantikan entri di atas)
- [2026-07-31] Paradigma penyajian data (dari proyek Pusat Valas, kini standar di sini juga): metrik adalah blok data tipografis tanpa border/fill — bukan stat card. Spec di `docs/blueprint/DATA_PRESENTATION.md`, disesuaikan ke palet nyata proyek ini (light, hijau forest; `--color-success` identik dengan `--color-primary`; `text-warning` terlalu terang untuk teks), `tabular-nums` (tidak ada utility `.tabular`), `Prisma.Decimal` → `.toNumber()` di service, dan admin berbahasa Indonesia inline tanpa namespace i18n. Prinsip "setiap angka adalah link" tetap dipertahankan lewat pola linked metric (§5) — yang dihapus kartunya, bukan href-nya. Primitif `Metric*` belum dibuat; §14 mendata halaman yang masih memakai stat card
- [2026-07-28] Empat prinsip flow admin (dipakai sebagai acuan setiap halaman admin baru): (1) sistem yang mengingatkan — antrean aksi di atas, statistik di bawah; (2) aksi ada di baris tempat datanya terlihat, bukan 2 klik ke halaman detail; (3) satu niat bisnis = satu tombol (Perpanjang / Pindah Kamar / Check-out, bukan "tutup lalu buat baru"); (4) setiap angka di dashboard adalah link ke list yang sudah terfilter
- [2026-07-28] Identitas periode tagihan adalah `Payment.periodStart`, bukan bulan kalender — `periodMonth/periodYear` tinggal kunci denormalisasi untuk laporan. Konsekuensinya kontrak harian/mingguan punya banyak periode dalam satu bulan dan akhirnya bisa ditagih; jatuh tempo memakai tanggal anniversary kontrak, bukan tanggal 5 global
- [2026-07-28] Aturan periode tagihan murni ada di `src/lib/billing.ts` (tanpa import prisma) supaya service dan unit test memakai implementasi yang sama — sebelumnya logikanya disalin ke file test dan berpotensi menyimpang
- [2026-07-28] Konteks properti aktif adalah state global admin: cookie `admin_property_id` lewat `getPropertyScope()` di `src/lib/property-scope.ts`, switcher di `admin/layout.tsx`. Halaman list tetap menerima `?propertyId` sebagai override eksplisit (deep link dari action queue) — pola: `getPropertyScope(searchParamPropertyId)`
- [2026-07-28] Untuk daftar panjang (penyewa/kamar) pakai `Combobox` di `src/components/ui/Combobox/` (dulu `SearchablePicker` lokal di `admin/contracts/`; ketik-untuk-filter + hidden input), bukan `<select>` — `<select>` berisi seluruh penyewa berhenti berguna di ~50 baris. Blacklist ditangani sebagai peringatan + checkbox konfirmasi yang mengunci tombol submit, bukan blokir keras

- [2026-07-28] Widget dashboard yang berisi angka/aksi harus di-gate memakai daftar role yang sama persis dengan layout guard halaman tujuannya — item antrean yang bisa dilihat tapi berujung `<Forbidden />` lebih buruk daripada tidak ditampilkan
- [2026-07-29] Sidebar admin memakai gaya ERP: baris nav tinggi tetap `h-11` (bukan `py-2`), penanda aktif berupa accent bar `bg-primary` di tepi kiri + `bg-primary-subtle`, dan collapse-to-icon-rail (`w-[4.5rem]`) yang dipersist di `admin_sidebar_collapsed`. Animasi buka/tutup section pakai trik `grid-rows-[1fr]`↔`[0fr]` — bukan `max-h-[...]` yang harus ditebak dan memotong section panjang
- [2026-07-28] Redesign halaman landing page public dengan UI bertumpuk (Stacked Floor Plan) untuk simulasi gedung hunian kost riil serta memperkaya section dengan visual premium nyata.
- [2026-07-31] **Fasilitas: modal add/edit + `IconPicker`.** `NewFacilityForm` inline diganti `FacilityFormDialog` (`admin/master-data/facilities/`) — satu komponen dua mode (`create`/`edit`) yang membedakan hanya Server Action yang di-bind (`createFacilityAction` vs `updateFacilityAction.bind(null, id)`), portal + `useDismissable` + deteksi sukses lewat `wasPending` ref (bukan field state khusus) mengikuti pola `DuplicateRoomDialog`. Trigger-nya lewat prop `renderTrigger: (open) => ReactNode` supaya tombol "Tambah Fasilitas" di halaman dan ikon pensil di `FacilityCard` bisa berbagi dialog yang sama tanpa dua salinan form. Field icon-nya sekarang `IconPicker` (`src/components/ui/IconPicker/`, promoted ke primitif karena bentuknya generik) — trigger `role="combobox"` (menghindari warning `jsx-a11y/role-supports-aria-props` yang muncul kalau dipasang di elemen `role="button"`), default menampilkan ~55 icon kurasi kost (disalin dari daftar di `prisma/seed.ts`) sebelum ada query, lalu jatuh ke pencarian substring atas seluruh `iconNames` dari `lucide-react/dynamic` (~1600 nama) begitu admin mengetik — bukan menampilkan semuanya sekaligus. `facilityService.update()` sudah ada sebelumnya tapi belum ada action/UI yang memanggilnya.

- [2026-06-06] Developer explicitly designed a two-tier learning system: THIS.md for general knowledge, LEARN.md for corrections — treat both as first-class project docs
- [2026-07-27] Pola guard route admin: `canAccess()` + `<Forbidden />` di layout/page, `requireRole()` (throw) di Server Action — jangan campur
- [2026-07-27] Entity yang punya relasi ke `AuditLog` (mis. `User`) tidak boleh hard delete — FK restrict akan menolak dan jejak audit hilang; pakai flag `isActive` seperti `Room`
- [2026-07-27] Rahasia (password, token) tidak pernah masuk payload `AuditLog` — cukup flag seperti `passwordChanged: true`
- [2026-07-27] Developer tidak mau container proyek lain diganggu saat port bentrok — pilih pindahkan port proyek ini (Postgres lokal sekarang di `5434`, bukan `5433`)
