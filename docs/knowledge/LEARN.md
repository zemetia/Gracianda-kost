# LEARN — Correction & Mistake Log

> Written whenever: (1) the user explicitly corrects the AI, or (2) the AI recognizes it deviated from what was asked or from best practice. Format is compact but lossless.

**Template:** `[YYYY-MM-DD] - [problem] - [solution] - [lesson]`

---

<!-- Entries below, newest first -->

[2026-07-31] - Form admin dinilai "sangat jelek, gak modern" padahal primitifnya baru dirapikan — akar masalahnya lapisan visual, bukan struktur: (a) field `bg-surface` L0.988 di atas `bg-background` L0.967, beda cuma ~2% sehingga field nyaris tak terbaca sebagai field; (b) `focus-visible:ring-offset-background` padahal field duduk di atas kartu, jadi offset-nya salah warna; (c) `id` dibuat dari label (`input-${label.toLowerCase()}`) — dua field berlabel sama menghasilkan `id` bentrok; (d) kontrol native (`<select>`, `type="date"`, checkbox `accent-primary`) membawa chrome OS yang mengabaikan seluruh token - Turunkan `--color-background` ke 0.958 & naikkan `--color-card` ke 0.995 supaya ada tangga elevasi, tambah token shadow bernada hijau-hangat (hitam netral bikin palet ini terlihat abu kotor), ganti `useId()`, buang ring-offset di field, dan tulis ulang semua kontrol native jadi kontrol kustom yang menulis ke hidden input - "Primitif sudah ada dan rapi" tidak sama dengan "tampilannya benar"; sebelum menyimpulkan form sudah beres, cek beda lightness antara field dan latarnya, dan cek kontrol mana yang masih native

[2026-07-31] - `cva` menggabungkan class dengan `clsx`, bukan `tailwind-merge` — variant `fieldSize: md` (`h-11`) dan variant `auto` (`h-auto`) sama-sama ikut terkirim, dan yang menang ditentukan urutan di stylesheet, bukan urutan class - Jadikan multi-line sebagai nilai dari variant yang sama (`fieldSize: 'auto'`), bukan variant boolean terpisah - Dua variant cva tidak boleh menyetel properti CSS yang sama; kalau kepikiran menambah variant boolean yang menimpa ukuran, itu tandanya nilainya harus masuk ke variant ukuran itu sendiri

[2026-07-31] - `peer-checked:` dipasang pada elemen yang bukan sibling dari input (`peer` di `<input>`, target di dalam `<span>` saudara) sehingga toggle chip tidak pernah berubah tampilan — Tailwind meng-generate `~` (general sibling), yang tidak menjangkau keturunan - Ganti ke `group-has-[:checked]:` dengan `group` di `<label>` pembungkus - `peer-*` hanya untuk sibling; untuk keturunan pakai `group-has-*`

[2026-07-31] - Test `Select`/`Combobox` gagal dengan `scrollIntoView is not a function` — jsdom tidak punya layout jadi metode itu tidak diimplementasi - Panggil opsional: `el?.scrollIntoView?.({ block: 'nearest' })` - API DOM yang bergantung layout (`scrollIntoView`, `getBoundingClientRect` yang bermakna, `IntersectionObserver`) harus dipanggil defensif kalau komponennya diuji di jsdom

[2026-07-30] - `npm run build` gagal: "Module '@prisma/client' has no exported member 'PrismaClient'" — Prisma 7 dengan `provider = "prisma-client-js"` (default lama) tidak lagi menulis deklarasi tipe yang valid ke `node_modules/@prisma/client` (`default.d.ts`/`.prisma/client/default.d.ts` kosong 0 byte, reproducible walau `prisma generate` diulang) - Ganti generator ke `provider = "prisma-client"` dengan `output = "../../src/generated/prisma"` eksplisit (wajib di Prisma 7), update semua import dari `@prisma/client` → `@/generated/prisma/client`, gitignore folder itu + `postinstall: "prisma generate"` di package.json, exclude `src/generated/**` dari ESLint - Prisma 7 tidak lagi men-generate client ke `node_modules` secara default; provider lama `prisma-client-js` masih "jalan" tanpa warning tapi diam-diam menghasilkan deklarasi tipe kosong — selalu pakai provider baru `prisma-client` + `output` eksplisit. Efek samping: `@auth/prisma-adapter@2.11.2` masih mengetik terhadap bentuk client lama, perlu `prisma as unknown as Parameters<typeof PrismaAdapter>[0]` di `src/auth.ts` sampai package itu update

[2026-07-28] - Rekomendasi flow admin disusun dari isi file yang ternyata basi (payment.service, dashboard.service, schema Prisma sudah jauh lebih maju di disk karena ada sesi agent lain menulis paralel) — separuh "friksi" yang dilaporkan sebenarnya sudah dikerjakan - Verifikasi ulang dengan Grep + `Get-ChildItem | Sort LastWriteTime` sebelum menyimpulkan, lalu lanjutkan hanya bagian yang benar-benar kurang - Sebelum mendiagnosis kondisi kode, cek mtime file dan konfirmasi isinya lewat Grep; di repo yang sedang disentuh proses lain, hasil baca bisa tertinggal dari disk

[2026-07-27] - Import 'buildRoomInquiryMessage' di RoomFloorPlan.tsx tidak digunakan dan memicu kegagalan type-check compiler strict - Hapus import yang tidak terpakai dan bersihkan kode - Selalu bersihkan import yang tidak terpakai agar tsc --noEmit keluar dengan exit code 0

[2026-07-27] - `requireRole()` dipakai langsung di page/layout admin, padahal fungsi itu `throw new Error('Forbidden')` — role yang tidak berhak dapat error boundary (500), bukan 403; dan di production `error.message` diredaksi jadi tidak bisa dibedakan dari crash biasa - Tambah `canAccess()` (non-throwing) di `src/lib/auth.ts` + komponen `<Forbidden />`; page/layout pakai `canAccess`, Server Action tetap pakai `requireRole` - Guard di UI dan guard di mutasi punya kebutuhan berbeda: UI harus merender penolakan, mutasi harus gagal keras

[2026-07-27] - `/admin/master-data/*` (kamar, fasilitas, promo) tidak punya layout guard sama sekali sejak Fase 1 — semua role bisa membuka list-nya, hanya Server Action mutasinya yang dijaga `requireRole` - Tambah `master-data/layout.tsx` → `SUPER_ADMIN|OPERASIONAL` - Setiap kali menambah section admin baru, guard route (layout) dan guard mutasi (action) harus dipasang berpasangan; jangan anggap guard mutasi sudah cukup

[2026-07-27] - 11 Server Action mutasi dari Fase 1-3 tidak menulis `AuditLog` (create/remove facility & promo, create floor, semua upload/hapus attachment, set blacklist) — jejak audit bolong padahal `auditService.log()` sudah ada sejak Fase 2 - Dilengkapi semua di Fase 6 - Audit log itu cross-cutting: tulis bersamaan dengan aksinya, bukan ditambal di fase belakangan; upload/hapus file juga mutasi data yang perlu tercatat

[2026-06-09] - Template used root `proxy.ts` (Next.js 16 rename of middleware.ts), which has confirmed production-mode bugs (#85711), Windows 11 `next start` failure (#85243), and Cloudflare/pageExtensions breakage (#86122, #86303) — reverted to `src/middleware.ts` with `export function middleware` — never use root `proxy.ts` in this template; always use `src/middleware.ts`; the `src/proxy/` modules folder is fine to keep

[2026-06-09] - i18n routing used `localePrefix: 'as-needed'` so the default locale had no URL prefix — `/about` served English without redirecting to `/en/about`, violating the "all routes must have a locale prefix" requirement; also `PostHogProvider` used `usePathname`/`useSearchParams` from `next/navigation` breaking the non-negotiable; blueprint docs documented the wrong localePrefix and still showed a deprecated `middleware.ts` example - Fixed `localePrefix` to `'always'`, fixed PostHog to use `@/i18n/navigation` + `useLocale`, and rewrote the blueprint sections - `localePrefix` must be `'always'`; never import from `next/navigation` even in analytics/provider files; blueprint must document `proxy.ts` not `middleware.ts`

[2026-06-06] - Pushed commits to `master` without first checking the repo's default branch (`main`), creating diverged histories - Merged `master` → `main` with `--allow-unrelated-histories`, pushed to `origin/main` - Always check the default branch before the first push; never assume it is `master`
